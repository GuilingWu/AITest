# Puzzle Textures

- Put each theme under `assets/textures/<theme_id>/`.
- Image files in a theme folder should start at `0` and increase by 1.
- Example: `assets/textures/scenery/0.png`, `1.png`, `2.png`.
